#+TITLE: 使用 LCEL 构建一个简单的 LLM 应用
#+HTML_HEAD: <link rel="stylesheet" type="text/css" href="../../css/main.css" />
#+HTML_LINK_UP: tutorial.html   
#+HTML_LINK_HOME: tutorial.html
#+OPTIONS: num:nil timestamp:nil ^:nil

在这个快速入门中，将展示如何使用 LangChain 构建一个简单的 LLM 应用
#+begin_example
  这个应用将把文本从英语翻译成另一种语言

  这是一个相对简单的 LLM 应用，只需一次 LLM 调用加上一些提示

  尽管如此，这是一个很好的开始使用 LangChain 的方式

  许多功能只需一些提示和一次 LLM 调用就可以构建！
#+end_example

阅读完本教程后，您将对以下内容有一个高层次的概述：
+ 使用 [[https://www.langchain.com.cn/docs/concepts/#chat-models][聊天模型]]
+ 使用 [[https://www.langchain.com.cn/docs/concepts/#prompt-templates][提示词模板]] 和 [[https://www.langchain.com.cn/docs/concepts/#output-parsers][输出解析器]]
+ 使用 [[https://www.langchain.com.cn/docs/concepts/#langchain-expression-language-lcel][LangChain 表达式]] *LCEL* 将组件串联在一起
+ 使用 [[https://www.langchain.com.cn/docs/concepts/#langsmith][LangSmith]] 调试和追踪您的应用
+ 使用 [[https://www.langchain.com.cn/docs/concepts/#langserve][LangServe]] 部署您的应用

* 安装
要安装 LangChain，请运行：

#+begin_src sh 
  pip install langchain
#+end_src

** LangSmith
#+begin_example
  使用 LangChain 构建的许多应用程序将包含多个步骤和多次调用大型语言模型

  随着这些应用变得越来越复杂，能够检查您的链或代理内部究竟发生了什么变得至关重要
#+end_example
做到这一点的最佳方法是使用 LangSmith

#+begin_src sh 
  export LANGCHAIN_TRACING_V2="true"
  export LANGCHAIN_API_KEY="..."
#+end_src

或者 在python中，可以使用以下方式设置：

#+begin_src python 
  import getpass
  import os

  os.environ["LANGCHAIN_TRACING_V2"] = "true"
  os.environ["LANGCHAIN_API_KEY"] = getpass.getpass()
#+end_src

* 使用语言模型

#+begin_example
首先，让我们学习如何单独使用语言模型
#+end_example
LangChain支持许多不同的语言模型，可以互换使用

** Anthropic
#+begin_src sh 
  pip install -qU langchain-anthropic
#+end_src

#+begin_src python 
  import getpass
  import os

  os.environ["ANTHROPIC_API_KEY"] = getpass.getpass()

  from langchain_anthropic import ChatAnthropic

  model = ChatAnthropic(model="claude-3-5-sonnet-20240620")
#+end_src

首先直接使用模型。*ChatModel* 是 LangChain _运行接口_ 的实例，这意味着它们提供了一个标准接口供我们与之交互。要简单地调用模型，可以将消息列表传递给 _.invoke_ 方法

#+begin_src python 
  from langchain_core.messages import HumanMessage, SystemMessage

  messages = [
      SystemMessage(content="Translate the following from English into Italian"),
      HumanMessage(content="hi!"),
  ]

  model.invoke(messages)
#+end_src

#+begin_example
AIMessage(content='ciao!', response_metadata={'token_usage': {'completion_tokens': 3, 'prompt_tokens': 20, 'total_tokens': 23}, 'model_name': 'gpt-4', 'system_fingerprint': None, 'finish_reason': 'stop', 'logprobs': None}, id='run-fc5d7c88-9615-48ab-a3c7-425232b562c5-0')
#+end_example

如果启用了LangSmith，可以看到此运行已记录到LangSmith

* 输出解析器

* 提示词模板

* 使用 LCEL 连接组件

* 使用 LangServe 提供服务



