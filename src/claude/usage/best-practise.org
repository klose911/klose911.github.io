#+TITLE: 最佳实践
#+HTML_HEAD: <link rel="stylesheet" type="text/css" href="../css/main.css" />
#+HTML_LINK_HOME: ./usage.html
#+HTML_LINK_UP: ./workflow.html
#+OPTIONS: num:nil timestamp:nil ^:nil

#+begin_example
从配置环境到跨并行会话扩展，充分利用 Claude Code 的提示和模式
#+end_example

Claude Code 是一个代理式编码环境。与等待回答问题的聊天机器人不同，Claude Code 可以读取文件、运行命令、进行更改，并在您观看、重定向或完全离开的情况下自主解决问题

#+begin_example
  这改变了工作方式。与其自己编写代码并要求 Claude 审查

  不如描述你想要什么，让 Claude 弄清楚如何构建它。Claude 会探索、规划和实现

  但这种自主性仍然伴随着学习曲线。Claude 在某些约束条件下工作，需要理解这些约束
#+end_example

本指南涵盖了在 Anthropic 内部团队和在各种代码库、语言和环境中使用 Claude Code 的工程师中已被证明有效的模式

#+begin_example
  有关代理循环如何在幕后工作的信息，请参阅 Claude Code 如何工作

  https://code.claude.com/docs/zh-CN/how-claude-code-works
#+end_example

大多数最佳实践都基于一个约束： *Claude 的 context window 填充速度很快，随着填充，性能会下降*
#+begin_example
  Claude 的 context window 保存整个对话，包括每条消息、Claude 读取的每个文件和每个命令输出

  但这可能会很快填满。单个调试会话或代码库探索可能会生成并消耗数万个 token
#+end_example

这很重要，因为当 context 填充时，LLM 性能会下降。当 context window 即将满时，Claude 可能会开始”遗忘”早期的指令或犯更多错误

#+begin_example
  context window 是最重要的资源

  要查看会话在实践中如何填充，请 观看交互式演练，了解启动时加载的内容以及每个文件读取的成本

  使用 自定义状态行 持续跟踪 context 使用情况，并查看 减少 token 使用 了解减少 token 使用的策略
#+end_example
* 给 Claude 一种验证其工作的方式


