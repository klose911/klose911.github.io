#+TITLE: 在 monorepo 或大型代码库中设置 Claude Code
#+HTML_HEAD: <link rel="stylesheet" type="text/css" href="../css/main.css" />
#+HTML_LINK_HOME: ../claude.html
#+HTML_LINK_UP: ../automation/automation.html
#+OPTIONS: num:nil timestamp:nil ^:nil

#+begin_example
  为 monorepo 和大型单树代码库配置 Claude Code

  使用嵌套的 CLAUDE.md 文件、稀疏 worktrees、代码智能和按包技能

  使 Claude 专注于正在处理的代码
#+end_example

大型代码库可以是拥有数百万行代码的单个存储库，也可以是包含许多包的 monorepo。Claude Code 可以在任何规模下工作，但随着代码库的增长，为较小项目调整的默认设置可能会用与任务无关的指令和文件读取填满上下文窗口，浪费 tokens 并降低 Claude 的性能

#+begin_example
  本指南向个人开发者和工程团队展示如何将 Claude 的范围限制在任务涉及的代码库部分

  每个部分都说明该设置是个人的还是提交到存储库的
#+end_example
* 本指南涵盖的内容


